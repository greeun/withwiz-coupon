# packages/coupon 과 상위 저장소가 모두 pnpm 워크스페이스 루트이므로, 이 파일이
# 없으면 pnpm 이 위로 올라가 그 워크스페이스에 설치한다. 이 예제를 독립 루트로
# 고정하기 위한 파일이다.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
